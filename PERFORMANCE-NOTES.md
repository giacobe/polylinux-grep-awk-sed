# Build performance revision

All large level data files are now generated by one BusyBox-compatible `awk` process per level rather than thousands of POSIX-shell loop iterations. Seed derivation, themes, record counts, target positions, evidence formats, instructions, and expected answers remain unchanged. `LAB_ID` remains `text-processing` for unified-grader compatibility.
