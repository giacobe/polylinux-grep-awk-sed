# PolyLinux grep, awk, and sed lab

Grader-compatible repaired package based on the supplied current implementation.

## Identity contract

- Submission LabId: `lab05-text-processing`
- Seed LAB_ID: `text-processing`
- Seed contract: `seed-v1`
- Theme catalog: `themes-v1`

## Runtime paths

- learner evidence: `/srv/text-processing/cases`
- root-only expected answers: `/var/lib/text-processing/answers`

Run as root with `sh install.sh`. For deterministic validation, provide the same email, date, Exercise secret, and per-level secrets used by the unified grader through `USER_ID`, `CURRENT_DATE`, `SYSTEM_PASSWORD`, and `LEVEL_PASSWORD_ROOT`.
